kind = "web"
previewPath = "/"
title = "Restaurant QR Ordering"
version = "1.0.0"
id = "artifacts/restaurant-qr"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21363

[services.development]
run = "pnpm --filter @workspace/restaurant-qr run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/restaurant-qr", "run", "build" ]
publicDir = "artifacts/restaurant-qr/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21363"
BASE_PATH = "/"
